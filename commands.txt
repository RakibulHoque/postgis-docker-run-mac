# GO TO THE WORKING DIR WHERE THE COMPOSE FILE IS LOCATED
docker-compose up
# GET THE POSTGIS CONTAINER NETWORK IP (using command `docker inspect <container_id>`, to get the container id, `docker ps`)
# RUN PGADMIN4 IN BROWSER
# SETUP DATABASE CONNECTION, HERE, REMEMBER TO USE THE IP ADDRESS OF THE POSTGIS CONTAINER FOUND FROM DOCKER INSPECT, OTHERWISE IT WONT CONNECT
